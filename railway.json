{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn lgsedu.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2",
    "preDeployCommand": "python manage.py migrate_schemas --shared && python manage.py migrate_schemas && python manage.py collectstatic --noinput",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
